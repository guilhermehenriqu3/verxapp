import {StyleSheet} from 'react-native';
import Colors from '../../Constants/Colors';

const styles = StyleSheet.create({
  container: {marginHorizontal: 10},
  input: {borderBottomWidth: 1, marginBottom: 40},
});

export default styles;
